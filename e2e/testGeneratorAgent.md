Check the test plan md file that contains the steps of performing the action on the browser on localhost:3000

While performing the action check the HTML selectors used to perform actions like button click, wait for selector, etc.

Use those selectors to generate test to be run on an electron app.

Take reference from send-welcome-santam-message.specs.ts how it opens the electron app and selects HTML selectors.

Generate the test code using this sample and the HTML selectors you will get while exploring the plan

Check something to know that the test has passed.

Close the browser after everything is done