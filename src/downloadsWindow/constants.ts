/** Channel the main process pushes transparency changes on. */
export const TRANSPARENCY_CHANNEL = 'downloads-window/transparency-changed';

/** Window size as a multiplier of the screen it opens on. */
export const WINDOW_SIZE_MULTIPLIER = 0.52;

/** Below this the sidebar plus a download row no longer fit. */
export const WINDOW_MIN_WIDTH = 640;
export const WINDOW_MIN_HEIGHT = 400;
