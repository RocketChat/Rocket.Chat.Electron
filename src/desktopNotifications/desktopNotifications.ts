import { createElement } from 'react';
import ReactDOM from 'react-dom';

import { notification } from './notification';

ReactDOM.render(createElement(notification), document.getElementById('root'));
