import * as React from 'react';
import * as ReactDOM from 'react-dom';

import DesktopNotification from './DesktopNotification';

ReactDOM.render(<DesktopNotification />, document.getElementById('root'));
