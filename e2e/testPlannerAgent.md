You will be generating a test plan for end to end regression testing

Don't need to create sub tests inside the test plan

Generate only 1 complete end to end test plan to acheive the task

check the seed.spec.ts file how it initializes the UI window

Use the same way to open up the browser in localhost:3000

Take snapshot at every page or after every action

When performing any action save the HTML tags or selectors used until you achieve the result

Use these selectors to create the test plan md file

You should save the selector code in the md file

At the end of all the actions take a final snapshot to check something to know if the requirements have been fulfilled.

Save the test plan in specs folder as a md file

Close the browser after exploration is done